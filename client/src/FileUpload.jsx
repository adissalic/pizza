import React from "react";

const FileUpload = () => {

    
  return (
    <div className="container">
      <input type="file" />
      <button>Upload</button>
    </div>
  );
};

export default FileUpload;
